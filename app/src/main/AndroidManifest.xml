<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mongk.alarmbirthcontrolpills">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".InfoActivity"
            android:label="ข้อมูลยาเม็ดคุมกำเนิด"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SolutionActivity"
            android:label="การแก้ใขเมื่อลืมรับประทาน" />
        <activity
            android:name=".activityEmptySetting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SetCalendarActivity" />
        <activity android:name=".GuideActivity" />
        <activity
            android:name=".NotificationActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".testActivity" />

        <receiver android:name=".AlarmReceiver" />

        <activity android:name=".CalendarActivity"></activity>
    </application>

</manifest>